# Godot 4+ specific ignores
.godot/
/android/

# Godot exports
# NOTE: export_presets.cfg is intentionally tracked so CI can build the Web export.
*.import.bak

# Web build output (generated locally / by CI)
build/

# OS / editor junk
.DS_Store
Thumbs.db
desktop.ini

# Redundant asset archive (the extracted folders are committed instead)
*.zip

# Local-only running decision log (Claude's working notes; never committed)
/DECISIONS.md
